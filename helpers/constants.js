const path = require('path');
const PUBLIC_PATH = path.join(__dirname, '../public');
const AVATAR_PATH = 'avatars';
const TMP_PATH = path.join(__dirname, '../tmp');

module.exports = { AVATAR_PATH, TMP_PATH, PUBLIC_PATH };
